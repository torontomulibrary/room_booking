The folders here contain the access for the Templates

The folder name MUST match the name of the template

For example, if using the theme application/views/rula_template.php, the folder name must be "rula"